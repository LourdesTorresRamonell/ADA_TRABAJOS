import { capitalize, reverse } from "./stringUtils";

let cadena:string=`la nena de Argentina`;
console.log(`Primera letra de ${cadena} es: ${capitalize(cadena)}`);
console.log(`La cadena: ${cadena}. Es: ${reverse(cadena)}`);